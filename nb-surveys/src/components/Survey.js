import {useState} from 'react';

const [state, setstate] = useState(initialState)

const Survey = () => {
    const [state, setstate] = useState(initialState)
    return (
        <div>
            <form>
                
            </form>
        </div>
    )

}